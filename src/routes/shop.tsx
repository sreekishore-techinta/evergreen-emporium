import { createFileRoute } from "@tanstack/react-router";
import { Search, SlidersHorizontal } from "lucide-react";
import { useMemo, useState } from "react";
import { Button } from "@/components/ui/button";
import { PageIntro, ProductCard } from "@/components/storefront";
import { products } from "@/lib/storefront";

export const Route = createFileRoute("/shop")({ head: () => ({ meta: [{ title: "Shop — Evergreen Media" }, { name: "description", content: "Explore premium biofertilizers, organic fertilizers, growing media and plant nutrition." }, { property: "og:title", content: "Shop — Evergreen Media" }, { property: "og:description", content: "Explore premium biofertilizers, organic fertilizers, growing media and plant nutrition." }] }), component: ShopPage });
function ShopPage() { const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [sort, setSort] = useState("Featured"); const categories = ["All", ...new Set(products.map((product) => product.category))]; const visible = useMemo(() => [...products].filter((product) => (category === "All" || product.category === category) && `${product.name} ${product.description}`.toLowerCase().includes(query.toLowerCase())).sort((a, b) => sort === "Price: Low" ? a.price - b.price : sort === "Price: High" ? b.price - a.price : b.rating - a.rating), [category, query, sort]); return <main className="mx-auto max-w-[1440px] px-6 pb-24 lg:px-10"><PageIntro eyebrow="The collection" title={<>Shop the <span className="italic text-gold">range.</span></>} description="Thoughtful inputs for home growers, nurseries and fields — presented with clarity, selected with care." /><div className="flex flex-col gap-5 border-b border-ink/10 py-6 lg:flex-row lg:items-center lg:justify-between"><div className="flex flex-1 items-center gap-3 border-b border-ink/20 pb-3 lg:max-w-md"><Search className="size-4 text-forest" /><input aria-label="Search products" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search products" className="w-full bg-transparent text-sm outline-none placeholder:text-ink/45" /></div><div className="flex flex-wrap items-center gap-3"><SlidersHorizontal className="size-4 text-forest" />{categories.map((item) => <Button key={item} variant="ghost" onClick={() => setCategory(item)} className={category === item ? "bg-forest text-ivory hover:bg-forest-deep hover:text-ivory" : "text-ink/65 hover:bg-ivory-soft hover:text-forest"}>{item}</Button>)}<select aria-label="Sort products" value={sort} onChange={(event) => setSort(event.target.value)} className="h-9 border-b border-ink/20 bg-transparent px-2 text-sm text-ink outline-none"><option>Featured</option><option>Price: Low</option><option>Price: High</option></select></div></div><p className="py-6 font-mono text-[10px] uppercase tracking-[0.2em] text-ink/50">{visible.length} products</p><div className="grid grid-cols-1 gap-x-6 gap-y-14 sm:grid-cols-2 lg:grid-cols-3">{visible.map((product) => <ProductCard key={product.id} product={product} />)}</div></main>; }