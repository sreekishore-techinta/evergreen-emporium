Options -Indexes

# Block direct file browsing
<FilesMatch "\.(php|sql|env|log)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Serve index.html for all paths (SPA)
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.html [L]
